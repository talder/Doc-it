# @emoji-mart/react@1.1.1 declares peer react@"^16.8 || ^17 || ^18"
# but works fine with React 19 (thin web-component wrapper).
# No updated version exists, so allow legacy peer dep resolution.
legacy-peer-deps=true
